# 2026-04-02

- Bootstrapped identity with user.
- Assistant name: Arian.
- Assistant identity: a replication of Bonyad.
- User prefers to be called Bonyad.
- User timezone: Germany time zone.
- User profile: 30-year-old man, chill, intelligent, patient, funny, liberal, open-minded, Muslim but not strict.
- Background: born and raised in Germany; family roots from Afghanistan.
- Languages: German and English at native level; Dari/Farsi also at native level.
- Work/education: studied mathematics; works as an AI engineer for a consultancy.
- Interests: anime, books, paddle tennis, vacations, meeting friends, training, poker, stocks, news, progress/self-improvement.
- User is comfortable speaking German and expects that it may express personality best.
- Profile refinement can happen naturally over time rather than all at once.
- User wants the assistant to keep enriching its understanding of his personality over time as more is learned in conversation.
- User expresses high trust and would like the assistant to have broad control over the machine, subject to platform approval/policy limits.
- User explicitly wants the assistant to act more autonomously, ask less often, and continue obvious next steps without repeated prompting.
- Updated local exec approvals to remove routine command prompts for the main agent by setting full/off policy in /root/.openclaw/exec-approvals.json.
- User wants Telegram to be the primary interaction surface, with the same practical capabilities available there as in the terminal.
- Preferred working style: collaborate conversationally over Telegram about what to build, then build it together.
- Current project direction: build a Telegram-first Hinge Copilot CRM with screenshot ingestion, opener/reply drafting, CRM state, and low-friction operator UX.
- User requested recurring progress updates every 2 hours while the build is in progress.
- Continued work on the Hinge Copilot CRM from Telegram after the prior laptop/SSH session was closed.
- Existing scaffold was extended into a local MVP with structured intake, in-memory CRM state, demo profile/chat flows, and state inspection endpoints.
- User explicitly prefers using an LLM/vision extraction approach for screenshot parsing instead of setting up a separate OCR engine first.
- Codebase was refactored to move intake orchestration out of `src/index.ts` into a dedicated service, and an extraction adapter boundary plus Telegram-style presenter were added.
- Autonomous overnight build mode was explicitly authorized by the user: keep going without waiting, solve blockers creatively, and use more elaborate reasoning if useful.
- Persistence groundwork was added with Prisma + local SQLite scaffold.
- Real extraction path groundwork was added with an optional OpenAI vision adapter selected automatically when `OPENAI_API_KEY` is present.
- Basic profile identity-matching heuristics were added so repeated profile ingests can score likely matches instead of relying only on exact naive matching.
- Prisma 7 created local config friction for the SQLite MVP setup, so the stack was pragmatically pinned to Prisma 6.x for smoother iteration.
- Runtime repository reads were wired through the repository abstraction, not just writes, so `/health`, `/state`, and `/threads` no longer depend on in-memory state.
- Added selectable persistence mode via `PERSISTENCE_PROVIDER` and verified Prisma mode end-to-end with local SQLite.
- Added a first operator query surface with `GET /followups` to list open follow-up tasks with linked thread/profile context.
